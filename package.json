{
  "name": "time-is-price",
  "version": "1.0.0",
  "description": "Time is Price — KPI·시간가계부·일정 PWA",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-app-icon-prefetch.mjs && node scripts/generate-pwa-splash-icons.mjs && vite build",
    "gen:icons": "node scripts/generate-app-icon-prefetch.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^5.4.0",
    "xlsx": "^0.18.5"
  }
}
