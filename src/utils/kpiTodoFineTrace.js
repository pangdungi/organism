/**
 * KPI 할일 미세 단계 (콘솔 비활성)
 */

export function kpiTodoFineTraceEnabled() {
  return false;
}

/** @param {string} _step @param {unknown} [_detail] */
export function kpiTodoFineTrace(_step, _detail) {}
