/**
 * 날짜 입력: 네이티브 input[type="date"] / datetime-local 통일 (Flatpickr 제거)
 * 패널 마운트 시 호출 호환용 no-op.
 */
export function initDatePicker(inputEl, _options = {}) {
  if (!inputEl) return null;
  return null;
}

export function initDatePickersIn(container) {
  if (!container) return;
}

export function observeDatePickerInit(container) {
  if (!container) return;
}
