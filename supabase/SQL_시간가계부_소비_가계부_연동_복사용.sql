-- ============================================================================
-- 시간가계부 「소비 기록」 ↔ 자산관리 「가계부」 — 복사·실행 안내
--
-- 결론: 새로 만들 테이블·트리거 없음. (가계부용 테이블을 그대로 공유)
--
-- Supabase에서 할 일
--   1) 웹 로그인 → 왼쪽 SQL Editor 클릭
--   2) New query(또는 빈 에디터)에서 아래 한 줄만 선택 후 Run 해도 되고,
--      이 파일 전체를 붙여넣고 Run 해도 됩니다. (주석만 있으면 일부 클라이언트는
--      문장이 없다고 할 수 있어, 확인용으로 select 한 줄을 넣었습니다.)
--
--   → 이미 asset_user_expense_transactions 테이블이 있으면 DB 추가 작업 없음.
--   → 앱(프론트) 배포가 반영되면 시간가계부에서 넣은 소비가 가계부에도 보입니다.
--      과제 기록에서 소비 태그 삭제 시 같은 id 거래가 가계부에서도 삭제·동기화됩니다.
-- ============================================================================

select 1 as db_ok_no_extra_migration_for_time_ledger_expense;
